<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.garcomfastmeal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".chamadas.ChamadasMesas"
            android:label="@string/title_activity_chamadas_mesas"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Pedidos.PedidosMesas"
            android:label="@string/title_activity_pedidos_mesas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".conexao.ui.detalhesfilme.DetalhesGarcomActivity" />
        <activity android:name=".conexao.ui.listafilmes.ListaGarcomActivity" />
        <activity android:name=".qrcode.LeitorQrcode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>